## ExecHub Evaluator Service

1. Start Server Command
```bash
npm run dev
```

2. If `npm run dev` not work
```bash
## Run first in separate terminal
npx tsc -w

## Run second in separate terminal
npx nodemon dist/index.js
```
